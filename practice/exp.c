/*checking if the given number is greater than 10 or not */
#include <stdio.h>
int main()
{
    int x;
if (x > 0)
    if (x < 10)
        printf("x is between 1 and 9\n");
    else
        printf("x is not less than 10\n");
}
