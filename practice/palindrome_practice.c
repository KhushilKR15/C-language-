#include <stdio.h>
#include <string.h>
int main()
{
    int a;
    char str1[100],rev[100];
    printf("enter the string you would like to check for palindrome\n");
    scaf("%s",&str1);
    strcmp(str1,rev);
    printf("the original string is ");
}